#include "main.h"

/**
 * mul - the function multiplies two numbers
 * @a: the first number
 * @b: the second number
 * Return: returns digit
 */
int mul(int a, int b)
{
	digit = a * b;
	return (digit);
}
