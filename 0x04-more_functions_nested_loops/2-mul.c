#include "main.h"

/**
 * mul - the function multiplies two numbers
 * @a: the first number
 * @b: the second number
 * Return: returns the product of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
