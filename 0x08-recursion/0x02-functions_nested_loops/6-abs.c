#include "main.h"

/**
 * _abs - the main function
 * @i: this is the number
 * Return: always returns the absolute value of i
 */
int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	else
	{
		return (i);
	}
}
