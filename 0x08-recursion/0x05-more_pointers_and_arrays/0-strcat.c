#include "main.h"

/**
 * _strcat - the main function
 * @dest: the pointer
 * @scr: the second pointer
 * Return: returns dest
 */
char*_strcat(char *dest, *scr)
{
	char i;
	dest = scr + i;
	return (dest);
}
